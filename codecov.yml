coverage:
  status:
    project:
      default: off
      frontend:
        target: 80%
        flags: frontend
      backend:
        target: 80%
        flags: backend

flags:
  frontend:
    paths:
      - frontend/
  backend:
    paths:
      - backend/
